# Dependencies — each build stage installs its own
**/node_modules/

# Compiled output — stages rebuild from source
**/dist/

# Secrets — never bake into the image
.env
**/.env
**/.env.*

# Editor / tooling
.vscode/
.idea/
.claude/
*.log
**/*.log

# Git history
.git/
